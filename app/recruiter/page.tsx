import { PageContainer } from "@/components/PageContainer";
import { PageHeader } from "@/components/PageHeader";

export default function RecruiterPage() {
  return (
    <main>
      <PageContainer>
        <PageHeader title="Recruiter Dashboard" description="This area will summarize hiring activity, open roles, and recent candidate engagement." />
      </PageContainer>
    </main>
  );
}
