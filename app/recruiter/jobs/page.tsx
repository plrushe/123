import { PageContainer } from "@/components/PageContainer";
import { PageHeader } from "@/components/PageHeader";

export default function RecruiterJobsPage() {
  return (
    <main>
      <PageContainer>
        <PageHeader title="Recruiter Jobs" description="This page will manage created job posts, drafts, and publishing workflow." />
      </PageContainer>
    </main>
  );
}
